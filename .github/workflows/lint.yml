name: super-linter

on:
  pull_request:
    branches: [ main ] 

jobs: 
  build: 
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Super-Linter
        uses: super-linter/super-linter@v7.1.0